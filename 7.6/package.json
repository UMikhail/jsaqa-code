{
  "name": "7.6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "chromeLaptop1": "npx cypress run --config-file cypress.config.js --browser chrome",
    "chromeLaptop2": "npx cypress run --config-file cypress.config.js --browser chrome --headed",
    "electronMobile1": "npx cypress run --config-file cypress.env.config.js --browser electron",
    "electronMobile2": "npx cypress run --config-file cypress.env.config.js --browser electron --headed"
  },
  "keywords": [
    "cypress"
  ],
  "author": "Mikhail",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.5.1"
  }
}
